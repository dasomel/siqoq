@AGENTS.md

# Siqoq Claude adapter

For simulation/recorded/physical sensor interfaces, vision inference, semantic events/event bus, edge-runtime portability, or early physical-AI pipeline changes, load `.agents/skills/siqoq-sim-to-edge-workflow/SKILL.md`.

The project skill is intentionally `draft` while the MVP contracts are still being validated. Keep Claude-specific orchestration outside the portable skill and do not add hardware assumptions to CLAUDE.md.

Use `make verify` as the CI-aligned deterministic baseline; add simulation or real-device evidence according to the property being claimed.
